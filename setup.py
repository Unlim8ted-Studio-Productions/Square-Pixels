from setuptools import setup, find_packages


setup(
    name="SquarePixel",
    version="1.0",
    packages=find_packages(),
    install_requires=["pygame", "pillow", "pymovie", "pyperclip", "playfab"],
)
